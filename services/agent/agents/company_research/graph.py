"""
Company Research Agent — Planner. Phase 2 — not yet implemented.

Dynamic planning:
  START → company_identifier
                ↓
          enough data in DB?
          YES → END
          NO  → web_search → playwright (if needed) → result_writer → END
"""
